# Copy this file to .env.local for local development. Never commit real credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/balloon_space?retryWrites=true&w=majority
MONGODB_DB_NAME=balloon_space

# Existing admin password used to protect Manage page write operations.
ADMIN_PASSWORD=<choose-a-strong-password>